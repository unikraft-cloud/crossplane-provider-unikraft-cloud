apiVersion: compute.kraftcloud.crossplane.io/v1alpha1
kind: Instance
metadata:
  name: example2
spec:
  forProvider:
    metro: dal0
    image: unikraft.io/nderjung.unikraft.io/http-go1.21/latest
    memory: "64Mi"
    args:
    - "/server"
    internalPort: 80
    port: 443
    desiredState: running
  providerConfigRef:
    name: example
